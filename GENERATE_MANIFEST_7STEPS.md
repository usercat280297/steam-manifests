# 📋 Hướng Dẫn Tự Động Lấy Manifest Game (7 Bước)

## ✅ Yêu Cầu Trước Tiên
- SteamCMD: `C:\steamcmd\steamcmd.exe`
- PowerShell (Windows)
- Git (tùy chọn, để push GitHub)

---

## 🎮 Ví Dụ: Silent Hill (AppID: 2947440)

### **BƯỚC 1: Tạo Script Tự Động**
Chạy lệnh này trong PowerShell:

```powershell
cd "E:\Đức Hải\steam-manifest-bot"

@'
param(
    [int]$AppId = 2947440,
    [string]$GameName = "Silent Hill"
)

Write-Host "`n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
Write-Host "🎮 STEAM MANIFEST GENERATOR v5.0" -ForegroundColor Cyan
Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`n" -ForegroundColor Cyan

$steamCmd = "C:\steamcmd\steamcmd.exe"

Write-Host "[STEP 1/7] Fetching app info from SteamCMD..." -ForegroundColor Yellow
$output = & $steamCmd "+login" "anonymous" "+app_info_print" $AppId "+quit" 2>&1 | Out-String

Write-Host "[STEP 2/7] Parsing depots..." -ForegroundColor Yellow
# Find all depot sections
$depotMatches = [regex]::Matches($output, '"(\d+)"\s*\n\s*{\s*"gid"\s+"(\d+)"')

$depots = @()
foreach ($match in $depotMatches) {
    $depotId = [int]$match.Groups[1].Value
    $manifestId = $match.Groups[2].Value
    
    # Filter only relevant depots (AppID and nearby)
    if ($depotId -gt 0 -and $manifestId -gt 0) {
        $depots += @{
            Id = $depotId
            Manifest = $manifestId
            Type = if ($depotId -eq $AppId -or $depotId -eq $AppId + 1) { "BASE" } else { "DLC" }
        }
    }
}

Write-Host "[STEP 3/7] Found $($depots.Count) depot(s)" -ForegroundColor Green

Write-Host "[STEP 4/7] Calculating SHA256 hashes..." -ForegroundColor Yellow
$lua = @"
-- ═══════════════════════════════════════════════════════════════════
-- $GameName
-- ═══════════════════════════════════════════════════════════════════
--
-- Generated by: Steam Manifest Bot v5.0
-- Steam App ID: $AppId
-- Total Depots: $($depots.Count)
--
-- ═══════════════════════════════════════════════════════════════════

addappid($AppId)

"@

$baseCount = 0
$dlcCount = 0

foreach ($depot in $depots) {
    $depotId = $depot.Id
    $manifestId = $depot.Manifest
    $depotType = $depot.Type
    
    # Calculate SHA256
    $hashInput = "$depotId`:$manifestId"
    $bytes = [System.Text.Encoding]::UTF8.GetBytes($hashInput)
    $sha256 = [System.Security.Cryptography.SHA256]::Create()
    $hash = $sha256.ComputeHash($bytes)
    $hashHex = [BitConverter]::ToString($hash) -replace '-', '' | ForEach-Object { $_.ToLower() }
    
    $lua += "-- $depotType Depot: $depotId (ManifestID: $manifestId)`n"
    $lua += "addappid($depotId, 0, `"$hashHex`")`n`n"
    
    if ($depotType -eq "BASE") { $baseCount++ } else { $dlcCount++ }
    
    Write-Host "  ✓ Depot $depotId ($depotType): $hashHex" -ForegroundColor Gray
}

$lua += "`n-- ═══════════════════════════════════════════════════════════════════`n"
$lua += "-- Base Depots: $baseCount | DLC Depots: $dlcCount`n"
$lua += "-- ═══════════════════════════════════════════════════════════════════`n"

Write-Host "[STEP 5/7] Saving manifest file..." -ForegroundColor Yellow
$outFile = "manifests\$AppId.lua"
$lua | Out-File -Encoding UTF8 -FilePath $outFile -Force

Write-Host "[STEP 6/7] File saved: $outFile" -ForegroundColor Green

Write-Host "[STEP 7/7] Ready to use!`n" -ForegroundColor Green
Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
Write-Host "📊 SUMMARY:" -ForegroundColor Cyan
Write-Host "  App: $GameName (ID: $AppId)" -ForegroundColor White
Write-Host "  Base Depots: $baseCount" -ForegroundColor White
Write-Host "  DLC Depots: $dlcCount" -ForegroundColor White
Write-Host "  Total: $($depots.Count)" -ForegroundColor White
Write-Host "  File: $outFile" -ForegroundColor White
Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`n" -ForegroundColor Cyan
'@ | Out-File -Encoding UTF8 -FilePath "auto-manifest-full.ps1" -Force

Write-Host "✅ Script created: auto-manifest-full.ps1" -ForegroundColor Green
```

---

### **BƯỚC 2: Chạy Script Cho Silent Hill**
```powershell
powershell -ExecutionPolicy Bypass -File auto-manifest-full.ps1 -AppId 2947440 -GameName "Silent Hill"
```

**Kết quả sẽ hiển thị:**
```
[STEP 1/7] Fetching app info from SteamCMD...
[STEP 2/7] Parsing depots...
[STEP 3/7] Found X depot(s)
[STEP 4/7] Calculating SHA256 hashes...
  ✓ Depot XXXXX (BASE): <hash>
  ✓ Depot XXXXX (DLC): <hash>
[STEP 5/7] Saving manifest file...
[STEP 6/7] File saved: manifests\2947440.lua
[STEP 7/7] Ready to use!

📊 SUMMARY:
  App: Silent Hill (ID: 2947440)
  Base Depots: X
  DLC Depots: X
  Total: X
  File: manifests\2947440.lua
```

---

### **BƯỚC 3: Kiểm Tra File**
```powershell
Get-Content manifests\2947440.lua
```

---

### **BƯỚC 4: Copy Vào GreenLuma**
```
Sao chép: manifests\2947440.lua
Dán vào: C:\path\to\GreenLuma\manifests\2947440.lua
```

---

### **BƯỚC 5: Thêm Vào GitHub (Tùy Chọn)**
```powershell
cd "E:\Đức Hải\steam-manifest-bot"
git add manifests\2947440.lua
git commit -m "Add Silent Hill (AppID 2947440) manifest with all DLCs"
git push origin main
```

---

### **BƯỚC 6: Lấy Link Tải**
Nếu push lên GitHub, link sẽ là:
```
https://raw.githubusercontent.com/usercat280297/steam-manifests/main/manifests/2947440.lua
```

---

### **BƯỚC 7: Restart Steam & Kiểm Tra**
- Đóng Steam hoàn toàn
- Mở GreenLuma SteamTool
- Chạy Game Unlock
- Mở Steam → Game sẽ hiện trong library

---

## 🎯 Sử Dụng Cho Bất Kỳ Game Nào

Thay đổi AppID và tên game:
```powershell
powershell -ExecutionPolicy Bypass -File auto-manifest-full.ps1 -AppId <APP_ID> -GameName "<GAME_NAME>"
```

**Ví dụ khác:**
```powershell
# Realm of the Mad God
powershell -ExecutionPolicy Bypass -File auto-manifest-full.ps1 -AppId 200210 -GameName "Realm of the Mad God"

# Counter-Strike 2
powershell -ExecutionPolicy Bypass -File auto-manifest-full.ps1 -AppId 730 -GameName "Counter-Strike 2"
```

---

## 📥 Link Tải Manifest

### Sushi House (AppID 3687470)
```
https://raw.githubusercontent.com/usercat280297/steam-manifests/main/manifests/3687470.lua
```

### Realm of the Mad God (AppID 200210)
```
https://raw.githubusercontent.com/usercat280297/steam-manifests/main/manifests/200210.lua
```

### Silent Hill (AppID 2947440)
```
https://raw.githubusercontent.com/usercat280297/steam-manifests/main/manifests/2947440.lua
```
(Sau khi chạy script và push lên GitHub)

---

## ⚙️ Troubleshooting

### ❌ Script không chạy?
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ❌ SteamCMD không tìm thấy?
- Tải SteamCMD: https://steamcdn-a.akamaihd.net/client/installer/steamcmd.zip
- Giải nén vào: `C:\steamcmd`

### ❌ Game không xuất hiện trong Steam?
1. Kiểm tra AppID có đúng không
2. Copy file lua vào folder GreenLuma/manifests chính xác
3. Restart Steam hoàn toàn

---

## 📝 Ghi Chú
- Manifest được tính toán tự động từ SteamCMD
- SHA256 hash = SHA256(DepotID:ManifestID)
- File lua có thể dùng cho GreenLuma, GreenLuma Reborn, v.v.
