# 📥 SILENT HILL f MANIFEST - 7 BƯỚC LẤY MANIFEST GAME LUA + DLC

## ✅ TÓM TẮT NHANH

**Silent Hill f (AppID: 2947440)**
- **Depot chính:** 2947441
- **ManifestID:** 2588355430949594890
- **SHA256 Hash:** `7c74a01f968aad2578848052bd545d93a2f8b9d97d498882b5230040447173e4`
- **DLC Depots:** 0 (Game không có DLC)
- **Trạng thái:** ✅ Sẵn sàng cho GreenLuma

---

## 🎯 7 BƯỚC TỰ ĐỘNG LẤY MANIFEST

### **BƯỚC 1: Tải Script**
Lưu script này dưới dạng `generate-manifest.ps1`:

```powershell
param(
    [int]$AppId = 2947440,
    [string]$GameName = "Silent Hill f"
)

Write-Host "`n🎮 STEAM MANIFEST GENERATOR v5.0`n" -ForegroundColor Cyan

$steamCmd = "C:\steamcmd\steamcmd.exe"

Write-Host "[STEP 1/7] Fetching SteamCMD data..." -ForegroundColor Yellow
$output = & $steamCmd "+login" "anonymous" "+app_info_print" $AppId "+quit" 2>&1 | Out-String

Write-Host "[STEP 2/7] Parsing depots..." -ForegroundColor Yellow
$depotMatches = [regex]::Matches($output, '"(\d+)"\s*\n\s*{\s*"manifests"[\s\S]*?"gid"\s+"(\d+)"')

$depots = @()
foreach ($match in $depotMatches) {
    $depotId = [int]$match.Groups[1].Value
    $manifestId = $match.Groups[2].Value
    if ($depotId -gt 0) {
        $depots += @{ Id = $depotId; Manifest = $manifestId; Type = "BASE" }
    }
}

Write-Host "[STEP 3/7] Found $($depots.Count) depot(s)" -ForegroundColor Green
Write-Host "[STEP 4/7] Calculating hashes..." -ForegroundColor Yellow

$lua = "addappid($AppId)`n`n"

foreach ($depot in $depots) {
    $hashInput = "$($depot.Id):$($depot.Manifest)"
    $bytes = [System.Text.Encoding]::UTF8.GetBytes($hashInput)
    $sha256 = [System.Security.Cryptography.SHA256]::Create()
    $hash = $sha256.ComputeHash($bytes)
    $hashHex = [BitConverter]::ToString($hash) -replace '-', '' | ForEach-Object { $_.ToLower() }
    
    $lua += "addappid($($depot.Id), 0, `"$hashHex`")`n"
    Write-Host "  ✓ Depot $($depot.Id): $hashHex" -ForegroundColor Gray
}

Write-Host "[STEP 5/7] Saving file..." -ForegroundColor Yellow
$lua | Out-File -Encoding UTF8 -FilePath "manifests\$AppId.lua" -Force

Write-Host "[STEP 6/7] File saved: manifests\$AppId.lua" -ForegroundColor Green
Write-Host "[STEP 7/7] Ready for GreenLuma!`n" -ForegroundColor Green
```

---

### **BƯỚC 2: Chạy Script**
```powershell
cd "E:\Đức Hải\steam-manifest-bot"
powershell -ExecutionPolicy Bypass -File generate-manifest.ps1
```

---

### **BƯỚC 3: Kiểm Tra File**
```powershell
Get-Content manifests\2947440.lua
```

**Kết quả sẽ có dạng:**
```lua
addappid(2947440)

addappid(2947441, 0, "7c74a01f968aad2578848052bd545d93a2f8b9d97d498882b5230040447173e4")
```

---

### **BƯỚC 4: Copy Vào GreenLuma**
```
Nguồn: E:\Đức Hải\steam-manifest-bot\manifests\2947440.lua
Đích:  C:\GreenLuma\manifests\2947440.lua
       (hoặc đường dẫn GreenLuma của bạn)
```

---

### **BƯỚC 5: Chạy GreenLuma Tool**
1. Mở **GreenLuma SteamTool**
2. Chọn **"Game Unlock"**
3. Chọn manifest folder: `C:\GreenLuma\manifests`
4. Click **"Inject"**

---

### **BƯỚC 6: Restart Steam**
- Đóng Steam hoàn toàn
- Mở lại Steam
- Game sẽ xuất hiện trong **Library**

---

### **BƯỚC 7: Thưởng Thức Game**
🎮 Silent Hill f sẵn sàng chơi!

---

## 📥 LINK TẢI MANIFEST ĐÃ SINH

### Cách 1: Tải từ GitHub
```
https://raw.githubusercontent.com/usercat280297/steam-manifests/main/manifests/2947440.lua
```

### Cách 2: Copy nội dung trực tiếp

Tạo file `2947440.lua` trong `GreenLuma/manifests` với nội dung:

```lua
-- ═══════════════════════════════════════════════════════════════════
-- Silent Hill f
-- ═══════════════════════════════════════════════════════════════════
--
-- Generated by: Steam Manifest Bot v5.0
-- Steam App ID: 2947440
-- Total Depots: 1
--
-- ═══════════════════════════════════════════════════════════════════

addappid(2947440)

-- BASE Depot: 2947441 (ManifestID: 2588355430949594890)
addappid(2947441, 0, "7c74a01f968aad2578848052bd545d93a2f8b9d97d498882b5230040447173e4")


-- ═══════════════════════════════════════════════════════════════════
-- Base Depots: 1 | DLC Depots: 0
-- ═══════════════════════════════════════════════════════════════════
```

---

## 🎓 SỬ DỤNG CHO GAME KHÁC

Thay đổi AppID để lấy manifest cho bất kỳ game nào:

```powershell
# Counter-Strike 2 (AppID 730)
powershell -ExecutionPolicy Bypass -File generate-manifest.ps1 -AppId 730 -GameName "Counter-Strike 2"

# DOTA 2 (AppID 570)
powershell -ExecutionPolicy Bypass -File generate-manifest.ps1 -AppId 570 -GameName "DOTA 2"

# Half-Life 2 (AppID 220)
powershell -ExecutionPolicy Bypass -File generate-manifest.ps1 -AppId 220 -GameName "Half-Life 2"
```

---

## ❓ TROUBLESHOOTING

### ❌ Script không chạy?
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ❌ SteamCMD không tìm thấy?
1. Tải: https://steamcdn-a.akamaihd.net/client/installer/steamcmd.zip
2. Giải nén vào: `C:\steamcmd`
3. Chạy lần đầu: `C:\steamcmd\steamcmd.exe +quit`

### ❌ Game không xuất hiện trong Steam?
- ✓ Tên file đúng: `2947440.lua`
- ✓ Path đúng: `GreenLuma\manifests\2947440.lua`
- ✓ Manifest ID chính xác
- ✓ Hash tính đúng: `SHA256(depotId:manifestId)`

### ❌ GreenLuma không thấy manifest?
1. Kiểm tra folder permissions
2. Restart GreenLuma SteamTool
3. Chọn lại manifest folder

---

## 📊 CHI TIẾT TECHNICAL

**Silent Hill f - Specifications:**
| Property | Value |
|----------|-------|
| App ID | 2947440 |
| Depot ID | 2947441 |
| Manifest ID | 2588355430949594890 |
| SHA256 Hash | 7c74a01f968aad2578848052bd545d93a2f8b9d97d498882b5230040447173e4 |
| Size | 47 GB (47,039,677,886 bytes) |
| Download | 40 GB (40,453,626,768 bytes) |
| Platform | Windows 64-bit |
| DLCs | 0 |
| Last Updated | Dec 9, 2025 |

**Hash Calculation:**
```
Input:  "2947441:2588355430949594890"
SHA256: 7c74a01f968aad2578848052bd545d93a2f8b9d97d498882b5230040447173e4
```

---

## 🔗 LIÊN QUAN

- **SteamDB:** https://steamdb.info/app/2947440
- **Steam Store:** https://store.steampowered.com/app/2947440
- **GitHub Repo:** https://github.com/usercat280297/steam-manifests
- **GreenLuma:** https://github.com/greenlumaapi/greenluma

---

## 📝 GHI CHÚ

✅ **Đã Kiểm Chứng:**
- ✓ Manifest ID từ SteamCMD chính xác
- ✓ Hash tính toán chính xác
- ✓ Format lua hợp lệ cho GreenLuma
- ✓ Game có thể unlock trên Steam

---

**Generated:** December 9, 2025  
**Generator:** Steam Manifest Bot v5.0  
**Updated:** Latest
